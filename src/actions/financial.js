import config from '../config';

export const fecthPortfolio = rankNumber => dispatch => {
	console.log('rankNumber:', rankNumber);

	// switch (this.props.id) {
 //      case 1:
	// 	return 
 //      case 2:
}